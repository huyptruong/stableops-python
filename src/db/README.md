# src/db

Optional folder for database logic: schema definitions, migrations, or DB-specific helpers.

In this template, SQLite setup and persistence currently live in **src/integrations/storage.py**. Use this folder when you add migrations, multiple tables, or want to separate schema from integration code.
