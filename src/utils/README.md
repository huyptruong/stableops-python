# src/utils

Shared helper functions that don’t belong to a single domain: formatting, parsing, date handling, etc.

Keep this folder minimal; if a helper is specific to one feature, consider putting it next to that feature in **src/services/** or **src/integrations/**.
