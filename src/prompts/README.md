# src/prompts

Place app-level AI prompt templates here (e.g. `.txt` or `.md` files, or Python modules that load prompt strings).

Keeping prompts separate from service code makes them easier to edit, test, and version. The app already uses prompts here (e.g. Create Social Post in `social_post.py`); add new files when you add workflows that need system or user prompt templates.
