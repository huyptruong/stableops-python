# src/prompts

Place app-level AI prompt templates here (e.g. `.txt` or `.md` files, or Python modules that load prompt strings).

Keeping prompts separate from service code makes them easier to edit, test, and version. The current demo sends user input directly to the LLM; add files here when you introduce system prompts or reusable templates.
