# Runtime dependencies for Streamlit Community Cloud and `pip install -r requirements.txt`.
# Kept in sync with pyproject.toml [project.dependencies]. Source of truth: pyproject.toml.
streamlit>=1.55
openai>=2.26
python-dotenv>=1.2
pydantic>=2.0
anthropic>=0.18
